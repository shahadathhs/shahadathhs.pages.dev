model Blog {
    id           String   @id @default(uuid())
    title        String
    slug         String   @unique
    excerpt      String
    category     String
    thumbnailUrl String
    content      String
    createdAt    DateTime @default(now())
    updatedAt    DateTime @updatedAt
}
